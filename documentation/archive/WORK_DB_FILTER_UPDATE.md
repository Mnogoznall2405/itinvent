# Обновление: Фильтрация по базам данных для работ

## Дата: 2025-10-21

## Описание изменений

Добавлена поддержка сохранения информации о базе данных и фильтрации по БД для функций "Работы" (замена картриджей и установка оборудования).

## Что изменилось

### 1. Сохранение базы данных (bot/handlers/work.py)

**Функции:**
- `save_cartridge_replacement()` - теперь сохраняет поле `db_name`
- `save_equipment_installation()` - теперь сохраняет поле `db_name`

**Логика:**
```python
# Получаем текущую БД пользователя
user_id = context._user_id if hasattr(context, '_user_id') else None
db_name = database_manager.get_user_database(user_id) if user_id else 'ITINVENT'

# Добавляем в запись
record = {
    'branch': '...',
    'location': '...',
    'db_name': db_name,  # ← Новое поле
    'timestamp': datetime.now().isoformat()
}
```

### 2. Экспорт с фильтрацией (bot/handlers/export.py)

**Обновленные функции:**

#### export_cartridges_to_excel()
```python
def export_cartridges_to_excel(only_new: bool = False, db_filter: str = None) -> str:
    # Фильтруем по БД если указан фильтр
    if db_filter:
        data = [item for item in data if item.get('db_name') == db_filter]
```

#### export_installations_to_excel()
```python
def export_installations_to_excel(only_new: bool = False, db_filter: str = None) -> str:
    # Фильтруем по БД если указан фильтр
    if db_filter:
        data = [item for item in data if item.get('db_name') == db_filter]
```

### 3. Интерфейс экспорта

**Добавлен выбор базы данных:**
- При экспорте замен картриджей
- При экспорте установок оборудования

**Опции:**
- 📦 Все базы (db_filter = None)
- 🏛 ITINVENT
- 🏛 MSK-ITINVENT
- 🏛 OBJ-ITINVENT
- 🏛 SPB-ITINVENT

### 4. Структура Excel файлов

**Новый порядок колонок для замен картриджей:**
1. Дата и время
2. **База данных** ← Новая колонка
3. Филиал
4. Локация
5. Модель принтера
6. Цвет картриджа

**Новый порядок колонок для установок:**
1. Дата и время
2. **База данных** ← Новая колонка
3. Филиал
4. Локация
5. Тип оборудования
6. Модель

### 5. Обратная совместимость

**Для старых записей без поля `db_name`:**
```python
# Добавляем db_name если отсутствует (для старых записей)
if 'db_name' not in df.columns:
    df['db_name'] = 'ITINVENT'
```

## Структура JSON файлов

### cartridge_replacements.json
```json
[
  {
    "branch": "г.Тюмень, Первомайская 19",
    "location": "19_105",
    "printer_model": "Xerox Versalink B605",
    "cartridge_color": "Черный",
    "db_name": "ITINVENT",
    "timestamp": "2025-10-21T23:01:01.935864"
  }
]
```

### equipment_installations.json
```json
[
  {
    "branch": "г.Тюмень, Первомайская 19",
    "location": "19_105",
    "equipment_type": "Монитор",
    "equipment_model": "Philips 243V7Q",
    "db_name": "ITINVENT",
    "timestamp": "2025-10-21T23:02:08.807188"
  }
]
```

## Пользовательский сценарий

### Регистрация работы
1. Пользователь выбирает БД (например, MSK-ITINVENT)
2. Регистрирует замену картриджа
3. Данные сохраняются с полем `"db_name": "MSK-ITINVENT"`

### Экспорт данных
1. Пользователь выбирает "Экспорт замен картриджей"
2. Выбирает период (все данные / только новые)
3. **Выбирает базу данных:**
   - Все базы - экспортирует все записи
   - MSK-ITINVENT - только записи из этой БД
4. Получает Excel файл с колонкой "База данных"

## Преимущества

✅ **Отслеживание источника данных** - видно из какой БД была запись  
✅ **Фильтрация при экспорте** - можно выгрузить данные по конкретной БД  
✅ **Обратная совместимость** - старые записи работают корректно  
✅ **Единообразие** - аналогично ненайденному оборудованию и перемещениям  

## Тестирование

### Тест 1: Сохранение с БД
1. Переключиться на MSK-ITINVENT
2. Зарегистрировать замену картриджа
3. Проверить в JSON наличие `"db_name": "MSK-ITINVENT"`

### Тест 2: Экспорт всех БД
1. Создать записи в разных БД
2. Экспортировать "Все базы"
3. Проверить наличие всех записей с колонкой "База данных"

### Тест 3: Экспорт конкретной БД
1. Экспортировать только MSK-ITINVENT
2. Проверить, что в файле только записи из MSK-ITINVENT

### Тест 4: Обратная совместимость
1. Экспортировать файл со старыми записями (без db_name)
2. Проверить, что они отображаются с "ITINVENT" по умолчанию

## Файлы изменены

- `bot/handlers/work.py` - добавлено сохранение db_name
- `bot/handlers/export.py` - добавлена фильтрация и колонка БД

## Связанные документы

- [WORK_FEATURE_SETUP.md](WORK_FEATURE_SETUP.md) - основная документация по функции "Работы"
- [FEATURES_SUMMARY.md](FEATURES_SUMMARY.md) - общий обзор функций бота
