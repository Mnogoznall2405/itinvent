# Руководство по тестированию новых функций

## Подготовка

1. Установите новую зависимость:
```bash
pip install python-docx
```

2. Перезапустите бота:
```bash
python -m bot.main
```

## Тест 1: Исправление переносов строк

**Цель:** Проверить, что все сообщения отображаются с правильными переносами строк

**Шаги:**
1. Отправьте боту `/start`
2. Выберите любую команду из меню
3. Проверьте, что текст сообщений отображается с переносами строк, а не с `\n`

**Ожидаемый результат:**
- Все сообщения читаемы
- Нет символов `\n` в тексте
- Форматирование корректное

---

## Тест 2: Команда /done при перемещении

**Цель:** Проверить, что команда `/done` работает после загрузки фотографий

**Шаги:**
1. Выберите "📦 Перемещение оборудования с актом"
2. Отправьте 1-2 фотографии оборудования с серийными номерами
3. Дождитесь распознавания серийных номеров
4. Отправьте команду `/done`

**Ожидаемый результат:**
- Бот запрашивает ФИО нового сотрудника
- Сообщение: "✅ Загружено X фото. Теперь укажите ФИО нового сотрудника..."

**Возможные проблемы:**
- Если ничего не происходит - проверьте, что бот перезапущен с новым кодом
- Если серийный номер не распознан - попробуйте другое фото

---

## Тест 3: Автоподсказки сотрудников

**Цель:** Проверить работу автоподсказок при вводе ФИО

**Шаги:**
1. Продолжите процесс перемещения (после `/done`)
2. Начните вводить ФИО сотрудника (минимум 2 символа)
   - Например: "Иван"
3. Дождитесь появления кнопок с подсказками

**Ожидаемый результат:**
- Появляются кнопки с ФИО сотрудников из базы
- Кнопка "✏️ Ввести как есть"
- Кнопка "🔄 Обновить список"

**Проверка функций:**

### 3.1. Выбор из списка
1. Нажмите на одну из кнопок с ФИО
2. Должно появиться подтверждение: "✅ Выбран сотрудник: [ФИО]"
3. Затем форма подтверждения перемещения

### 3.2. Ввод вручную
1. Нажмите "✏️ Ввести как есть"
2. Должно принять введённый текст
3. Переход к подтверждению

### 3.3. Обновление списка
1. Нажмите "🔄 Обновить список"
2. Список подсказок должен обновиться
3. Можно выбрать из нового списка

**Возможные проблемы:**
- Если подсказки не появляются - проверьте, что в БД есть сотрудники с таким именем
- Если ошибка - проверьте логи бота

---

## Тест 4: Генерация PDF-акта

**Цель:** Проверить создание PDF-акта приема-передачи

**Шаги:**
1. Завершите процесс перемещения (выберите сотрудника)
2. Проверьте данные в форме подтверждения:
   - Новый сотрудник
   - Количество единиц
   - Серийные номера
3. Нажмите "✅ Подтвердить"

**Ожидаемый результат:**
- Сообщение: "🛠️ Акт приема-передачи создается..."
- Бот отправляет PDF-файл
- Сообщение: "✅ Перемещение оборудования завершено!"

**Проверка PDF:**
1. Откройте полученный PDF-файл
2. Проверьте содержимое:
   - ✅ Заголовок "АКТ ПРИЕМА-ПЕРЕДАЧИ ОБОРУДОВАНИЯ"
   - ✅ Номер и дата акта
   - ✅ База данных
   - ✅ Таблица с оборудованием (серийные номера, модели)
   - ✅ "От кого" и "К кому"
   - ✅ Таблица подписей
   - ✅ Дата составления

**Возможные проблемы:**

### Ошибка: "No module named 'docx'"
**Решение:**
```bash
pip install python-docx
```

### Получен DOCX вместо PDF
**Причина:** Ошибка конвертации (требуется Microsoft Word на Windows)

**Решение:**
- Откройте DOCX в Word
- Сохраните как PDF вручную
- Или установите Word (если его нет)

### Ошибка при создании акта
**Проверьте:**
1. Логи бота: `type bot.log` или `Get-Content bot.log -Tail 50`
2. Наличие директории `transfer_acts/`
3. Права на запись в директорию

---

## Тест 5: Полный цикл перемещения

**Цель:** Проверить весь процесс от начала до конца

**Шаги:**
1. Выберите "📦 Перемещение оборудования с актом"
2. Отправьте 2-3 фотографии оборудования
3. Проверьте, что серийные номера распознаны
4. Отправьте `/done`
5. Введите начало ФИО (например, "Петр")
6. Выберите сотрудника из подсказок
7. Проверьте данные в подтверждении
8. Нажмите "✅ Подтвердить"
9. Получите и проверьте PDF-акт

**Ожидаемый результат:**
- Весь процесс проходит без ошибок
- PDF-акт содержит все данные
- Данные сохранены в `equipment_transfers.json`

---

## Проверка логов

Если что-то не работает, проверьте логи:

### Windows CMD:
```bash
type bot.log
```

### Windows PowerShell:
```powershell
Get-Content bot.log -Tail 100
```

### Мониторинг в реальном времени:
```powershell
Get-Content bot.log -Wait -Tail 50
```

---

## Отчёт о проблемах

Если обнаружили проблему, укажите:

1. **Что делали:** Описание шагов
2. **Что ожидали:** Ожидаемый результат
3. **Что получили:** Фактический результат
4. **Логи:** Последние строки из bot.log
5. **Скриншоты:** Если возможно

---

## Успешное тестирование

Если все тесты пройдены успешно:

✅ Переносы строк исправлены  
✅ Команда /done работает  
✅ Автоподсказки сотрудников работают  
✅ PDF-акты генерируются корректно  
✅ Полный цикл перемещения работает  

**Бот готов к использованию!** 🎉
