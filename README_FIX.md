# Исправление сохранения моделей компонентов

## Описание проблемы

При выборе фьюзера (или других компонентов) через интерфейс "Работы" модель компонента не сохранялась в JSON файл. В поле `cartridge_model` записывалась пустая строка вместо конкретной модели из базы данных.

## Причина проблемы

В функции `handle_component_selection` в файле `bot/handlers/work.py` при выборе компонента (не картриджа) не производился поиск модели в базе данных картриджей.

## Решение

### 1. Добавлена функция поиска модели компонента

```python
async def lookup_component_model(printer_model: str, component_type: str) -> str:
    """Ищет модель компонента в базе данных картриджей"""
```

### 2. Обновлен обработчик выбора компонента

При выборе non-cartridge компонента теперь ищется его модель в базе данных:
```python
if component_type != 'cartridge':
    printer_model = context.user_data.get('work_printer_model', '')
    component_model = await lookup_component_model(printer_model, component_type)
    if component_model:
        context.user_data['work_cartridge_model'] = component_model
```

### 3. Обновлено подтверждение замены

Теперь в подтверждении показывается найденная модель компонента:
```
📦 Модель фьюзера (печки): 115R00334
```

## Тестирование

1. Запустите бота: `python -m bot.main`
2. Выполните команду `/work`
3. Выберите "Замена комплектующих"
4. Введите серийный номер принтера (например, с Xerox Versalink C7120)
5. Выберите "🔥 Фьюзер (печка)"
6. Проверьте, что в подтверждении указана модель фьюзера
7. После сохранения проверьте файл `data/cartridge_replacements.json`

## Результат

Теперь при выборе фьюзера/фотобарабана/контейнера их точные модели сохраняются в JSON файл и отображаются в экспорте.