# Функция: Отправка актов старым владельцам

## Дата: 22.10.2024

## Описание

Добавлена новая функция автоматической отправки актов приема-передачи старым владельцам оборудования (сотрудникам, на которых была техника).

## Как это работает

### 1. После создания актов

После успешного создания множественных актов приема-передачи пользователь видит три кнопки:

```
✅ Перемещение оборудования завершено!

📄 Создано актов: 3
📦 Всего единиц оборудования: 5
👤 Новый владелец: Новиков Новик Новикович

Все акты отправлены вам в чат.

Хотите отправить все акты на email?

[📧 Отправить старым владельцам]
[✉️ Ввести email вручную]
[⏭ Пропустить]
```

### 2. Кнопка "📧 Отправить старым владельцам"

При нажатии на эту кнопку:

1. **Система получает email каждого старого владельца из БД**
   - Сначала пробует точное совпадение (strict=True)
   - Если не найдено, пробует нечеткий поиск (strict=False)

2. **Отправляет каждому владельцу его акт**
   - Каждый сотрудник получает только свой акт
   - Письмо персонализировано (обращение по имени)
   - Тема: "Акт приема-передачи оборудования: [filename]"

3. **Показывает результат отправки**
   - Список успешно отправленных актов
   - Список неудачных отправок с причинами
   - Рекомендации по дальнейшим действиям

## Примеры использования

### Сценарий 1: Все email найдены и отправлены

```
✅ Все акты успешно отправлены!

📧 Отправлено актов: 3

Получатели:
  • Иванов Иван Иванович → ivanov@company.com
  • Петров Петр Петрович → petrov@company.com
  • Сидоров Сидор Сидорович → sidorov@company.com
```

### Сценарий 2: Частичная отправка

```
⚠️ Акты отправлены частично

✅ Успешно отправлено: 2
  • Иванов Иван Иванович → ivanov@company.com
  • Петров Петр Петрович → petrov@company.com

❌ Не удалось отправить: 1
  • Сидоров Сидор Сидорович (Email не найден в БД)

💡 Для неотправленных актов используйте 'Ввести email вручную'
```

### Сценарий 3: Все отправки неудачны

```
❌ Не удалось отправить ни одного акта

Причины:
  • Иванов Иван Иванович: Email не найден в БД
  • Петров Петр Петрович: Ошибка отправки email
  • Сидоров Сидор Сидорович: Email не найден в БД

💡 Используйте 'Ввести email вручную' для отправки

[✉️ Ввести email вручную]
[⏭ Пропустить]
```

## Формат письма

### Тема письма:
```
Акт приема-передачи оборудования: transfer_act_20241022_143000_Ivanov_Ivan_Ivanovich.pdf
```

### Тело письма:
```
Добрый день, Иванов Иван Иванович!

Во вложении акт приема-передачи оборудования.

Пожалуйста, подпишите его и отправьте подписанный отсканированный акт ответным письмом.

Спасибо!
```

### Вложение:
- PDF-файл акта приема-передачи для конкретного сотрудника

## Технические детали

### Получение email из БД

Используется метод `get_owner_email()` из `universal_database.py`:

```python
# Сначала точное совпадение
owner_email = user_db.get_owner_email(old_employee, strict=True)

# Если не найдено - нечеткий поиск
if not owner_email:
    owner_email = user_db.get_owner_email(old_employee, strict=False)
```

### Отправка email

Используется `EmailSender` с асинхронной отправкой:

```python
email_sender = EmailSender()
success = await asyncio.to_thread(
    email_sender.send_files,
    recipient_email=owner_email,
    files={'act_pdf': pdf_path},
    subject=subject,
    body=body
)
```

### Обработка ошибок

Система обрабатывает следующие ошибки:
- Email не найден в БД
- Ошибка отправки email (SMTP)
- Файл акта не найден
- Ошибка подключения к БД

## Логирование

Все операции логируются:

```python
logger.info(f"Акт успешно отправлен {old_employee} на {owner_email}")
logger.error(f"Не удалось отправить акт {old_employee}")
logger.warning(f"Email не найден для {old_employee}")
```

## Очистка файлов

После успешной отправки всех актов:
- PDF-файлы автоматически удаляются
- Данные очищаются из `context.user_data`
- Пользователь возвращается в главное меню

При частичной отправке:
- Файлы НЕ удаляются
- Пользователь может попробовать отправить вручную

## Альтернативные варианты

### Кнопка "✉️ Ввести email вручную"

Если автоматическая отправка не удалась или нужно отправить все акты одному получателю:

1. Пользователь вводит email адрес
2. Все акты отправляются одним письмом
3. Получатель получает все PDF во вложениях

### Кнопка "⏭ Пропустить"

Если отправка не нужна:
- Все PDF-файлы удаляются
- Данные очищаются
- Возврат в главное меню

## Требования

### База данных

Таблица `OWNERS` должна содержать поле `OWNER_EMAIL`:

```sql
SELECT OWNER_EMAIL 
FROM OWNERS 
WHERE OWNER_DISPLAY_NAME = 'Иванов Иван Иванович'
```

### SMTP-сервер

Должен быть настроен в `.env`:

```bash
SMTP_SERVER=10.103.0.51
SMTP_PORT=25
EMAIL_ADDRESS=noreply@company.com
```

## Тестирование

### Тест 1: Все email найдены

1. Выполните перемещение с 2-3 группами
2. Убедитесь, что у всех старых владельцев есть email в БД
3. Нажмите "📧 Отправить старым владельцам"
4. **Ожидаемый результат:** Все акты отправлены успешно

### Тест 2: Некоторые email не найдены

1. Выполните перемещение, включая сотрудника без email
2. Нажмите "📧 Отправить старым владельцам"
3. **Ожидаемый результат:** Частичная отправка, список неудачных

### Тест 3: SMTP недоступен

1. Временно отключите SMTP-сервер
2. Попробуйте отправить акты
3. **Ожидаемый результат:** Ошибка отправки, предложение ввести email вручную

## Проверка логов

```powershell
# Поиск отправок актов
Select-String -Path bot.log -Pattern "Акт успешно отправлен|Не удалось отправить акт" -Context 1

# Поиск ошибок email
Select-String -Path bot.log -Pattern "Email не найден для" -Context 2
```

## Преимущества

1. **Автоматизация** - не нужно вручную вводить email каждого сотрудника
2. **Персонализация** - каждый получает только свой акт
3. **Надежность** - обработка ошибок и fallback на ручной ввод
4. **Прозрачность** - детальный отчет о результатах отправки
5. **Гибкость** - возможность выбрать альтернативный способ отправки

## Ограничения

1. Email должен быть в таблице OWNERS
2. SMTP-сервер должен быть доступен
3. Максимум 10 актов за одну операцию (ограничение системы)

## Будущие улучшения

- [ ] Возможность редактировать email перед отправкой
- [ ] Групповая отправка (несколько получателей для одного акта)
- [ ] История отправок с возможностью повторной отправки
- [ ] Шаблоны писем для разных типов актов
- [ ] Уведомления о прочтении письма

## Связанные документы

- `docs/ACT_EMAIL_FEATURE.md` - Основная функция отправки актов
- `docs/HOTFIX_TRANSFER_EMAIL_SUGGESTIONS.md` - Исправления email и подсказок
- `docs/MULTI_EMPLOYEE_TRANSFER_TESTING.md` - Тестирование множественных актов
