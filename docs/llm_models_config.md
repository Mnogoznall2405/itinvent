# ⚙️ Настройка LLM моделей для анализа картриджей

## 📍 Где настраивать?

LLM модель для анализа замен картриджей настраивается в файле `.env`:

```bash
CARTRIDGE_ANALYSIS_MODEL=anthropic/claude-3.5-sonnet
```

## 🤖 Доступные модели

### 🏆 Рекомендуемые модели

#### 1. **anthropic/claude-3.5-sonnet** (по умолчанию)
- ✅ Лучшая для структурирования данных
- ✅ Отлично работает с JSON
- ✅ Понимает русские названия филиалов
- 💰 Средняя стоимость

#### 2. **anthropic/claude-3-haiku**
- ✅ Очень быстрая
- ✅ Дешевая
- ⚠️ Может менее точно структурировать сложные данные

### 🔄 Альтернативы

#### OpenAI модели:
```bash
CARTRIDGE_ANALYSIS_MODEL=openai/gpt-4o          # Мощная, универсальная
CARTRIDGE_ANALYSIS_MODEL=openai/gpt-4o-mini     # Дешевая версия
```

#### Google модели:
```bash
CARTRIDGE_ANALYSIS_MODEL=google/gemini-1.5-pro  # Хорошая аналитика
```

#### Открытые модели:
```bash
CARTRIDGE_ANALYSIS_MODEL=meta-llama/llama-3.1-405b  # Большая открытая
```

## 💰 Сравнение стоимости (за 1000 запросов)

| Модель | Примерная стоимость | Скорость | Качество |
|--------|-------------------|----------|----------|
| claude-3.5-sonnet | $3-5 | Средняя | Отличное |
| claude-3-haiku | $0.5-1 | Быстрая | Хорошее |
| gpt-4o | $5-7 | Быстрая | Отличное |
| gpt-4o-mini | $1-2 | Очень быстрая | Хорошее |
| gemini-1.5-pro | $2-3 | Средняя | Отличное |

## 📝 Как изменить модель

### Способ 1: Через .env файл
```bash
# Откройте .env файл
CARTRIDGE_ANALYSIS_MODEL=anthropic/claude-3-haiku
```

### Способ 2: Временно для тестирования
```python
# В bot/handlers/export.py временно:
model="anthropic/claude-3-haiku"  # Вместо config.api.cartridge_analysis_model
```

## 🚀 Рекомендации

### Для продакшена:
- **anthropic/claude-3.5-sonnet** - лучший выбор
- Стабильное качество JSON ответов

### Для экономии:
- **anthropic/claude-3-haiku** - если бюджет ограничен
- **openai/gpt-4o-mini** - хорошая альтернатива

### Для экспериментов:
- **google/gemini-1.5-pro** - попробовать альтернативу
- **meta-llama/llama-3.1-405b** - если нужна большая модель

## ⚠️ Важные замечания

1. **API ключ должен быть активен** для OpenRouter
2. **Модель должна поддерживать JSON** форматирование
3. **Temperatute установлена в 0.1** для стабильных результатов
4. **Система автоматически падбэкается** на базовый отчет если LLM недоступен

## 🔍 Логирование

Все запросы к LLM логируются в `bot.log`:
- Успешные запроси: `logger.info()`
- Ошибки: `logger.error()`

## 🧪 Тестирование новой модели

```bash
# Тестируем с новой моделью
python test_cartridge_export.py

# Проверяем логи
tail -f bot.log | grep "LLM"
```

---

**Готово!** Теперь вы можете легко настраивать любую LLM модель через конфигурацию. 🎉