# IT-invent Bot v2.0 - Модульная архитектура

Telegram-бот для управления инвентаризацией IT-оборудования.

## ✨ Возможности

- 🔎 **Поиск оборудования** - по серийному номеру (текст/фото) или сотруднику
- 📝 **Ненайденное оборудование** - добавление информации об оборудовании вне БД
- 📦 **Перемещение с актом** - генерация PDF-актов приема-передачи
- 🗄️ **Управление БД** - работа с несколькими базами данных
- 📊 **Экспорт данных** - выгрузка в Excel/CSV с отправкой на email
- 🤖 **OCR** - автоматическое распознавание серийных номеров с фото

## 📁 Структура проекта

```
bot/
├── __init__.py              # Инициализация пакета
├── main.py                  # Точка входа (запуск бота) ✓
├── config.py                # Конфигурация и константы ✓
├── handlers/                # Обработчики команд
│   ├── __init__.py         ✓
│   ├── start.py            # /start, /help, /cancel ✓
│   ├── search.py           # Поиск по серийному номеру ✓
│   ├── employee.py         # Поиск по сотруднику ✓
│   ├── transfer.py         # Перемещение оборудования ✓
│   ├── database.py         # Управление БД ✓
│   ├── export.py           # Экспорт данных ✓
│   └── unfound.py          # Ненайденное оборудование ✓
├── services/                # Бизнес-логика
│   ├── __init__.py         ✓
│   ├── validation.py       # Валидация данных ✓
│   ├── ocr_service.py      # OCR через OpenRouter ✓
│   └── pdf_generator.py    # Генерация PDF-актов ✓
├── utils/                   # Вспомогательные функции
│   ├── __init__.py         ✓
│   ├── decorators.py       # @require_user_access и др. ✓
│   ├── keyboards.py        # Создание клавиатур ✓
│   ├── formatters.py       # Форматирование сообщений ✓
│   └── pagination.py       # Пагинация результатов ✓
└── README.md               # Эта документация
    └── __init__.py
```

## Запуск бота

### Из корневой директории проекта:
```bash
python -m bot.main
```

### Или напрямую:
```bash
python bot/main.py
```

## Что уже реализовано (Фаза 1)

✅ **Структура директорий** - создана модульная организация  
✅ **config.py** - централизованная конфигурация с dataclass  
✅ **utils/decorators.py** - @require_user_access, @log_execution_time, @handle_errors  
✅ **utils/keyboards.py** - функции создания клавиатур  
✅ **utils/formatters.py** - форматирование сообщений  
✅ **utils/pagination.py** - утилиты пагинации  
✅ **services/validation.py** - валидация пользовательских данных  
✅ **handlers/start.py** - команды /start, /help, /cancel  
✅ **main.py** - точка входа с ротацией логов  

## Реализованные функции

✅ **services/ocr_service.py** - OCR через OpenRouter API  
✅ **services/pdf_generator.py** - генерация PDF-актов приема-передачи  
✅ **handlers/search.py** - поиск по серийному номеру  
✅ **handlers/employee.py** - поиск по сотруднику  
✅ **handlers/transfer.py** - перемещение оборудования с актом  
✅ **handlers/database.py** - управление базами данных  
✅ **handlers/export.py** - экспорт данных  
✅ **handlers/unfound.py** - ненайденное оборудование  

## Ключевые улучшения

### 1. Ротация логов
- Автоматическая ротация при достижении 10 МБ
- Хранение последних 5 файлов
- Предотвращает переполнение диска

### 2. Централизованная конфигурация
- Все настройки в одном месте (config.py)
- Использование dataclass для группировки
- Валидация обязательных параметров при загрузке

### 3. Переиспользуемые утилиты
- Декораторы для проверки доступа и логирования
- Функции создания клавиатур
- Форматирование сообщений

### 4. Валидация данных
- Проверка серийных номеров
- Валидация ФИО
- Проверка IP-адресов
- Защита от SQL-инъекций

### 5. Генерация PDF-актов
- Автоматическое создание актов приема-передачи
- Заполнение таблиц с данными об оборудовании
- Конвертация DOCX в PDF
- Уникальная нумерация по дате и времени
- Fallback на DOCX при ошибке конвертации

## Миграция с bot.py

Старый файл сохранен как `bot_legacy.py` для справки.

### Что изменилось:
- Код разбит на логические модули
- Улучшена читаемость и поддерживаемость
- Добавлена ротация логов
- Централизована конфигурация
- Улучшена валидация данных

### Обратная совместимость:
- Все команды работают как раньше
- JSON-файлы данных не изменились
- Формат сообщений сохранен

## Разработка

### Добавление нового обработчика:
1. Создайте файл в `bot/handlers/`
2. Импортируйте необходимые утилиты
3. Добавьте обработчик в `bot/handlers/__init__.py`
4. Зарегистрируйте в `bot/main.py`

### Добавление нового сервиса:
1. Создайте файл в `bot/services/`
2. Реализуйте бизнес-логику
3. Экспортируйте в `bot/services/__init__.py`
4. Используйте из обработчиков

## Тестирование

```bash
# Проверка импортов
python -c "from bot.config import config; print('OK')"

# Проверка обработчиков
python -c "from bot.handlers import start; print('OK')"

# Запуск бота
python -m bot.main
```

## Логирование

Логи сохраняются в:
- `bot.log` - текущий лог
- `bot.log.1` - `bot.log.5` - архивные логи

Просмотр логов:
```bash
# Последние 100 строк
powershell Get-Content bot.log -Tail 100

# Мониторинг в реальном времени
powershell Get-Content bot.log -Wait -Tail 50
```
